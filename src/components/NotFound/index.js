import React from 'react';

import './styles.scss';

const NotFound = () => (
  <section className="not-found">
    <h1 className="title">Page Not Found</h1>
    <span className="not-found__message">Nothing to see here friend!!!</span>
  </section>
);

export default NotFound;
